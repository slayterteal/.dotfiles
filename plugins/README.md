
The plugins folder contains any code package that can be loosely called a "plugin" (shocker I know).

The intended purpose is to use this folder to store work-in-progress or custom plugins I do not otherwise want to add to it's own Git repo. Eventually any project of significant complexity should end up in it's own Git repo.

*This folder exists for my on convenience.*
